// import { Navigate, Route } from "react-router-dom";

// const ProtectedRoute = ({ element: Element, isAuthenticated, path }) => {
//     if (!isAuthenticated) {
//         return <Navigate to="/login" />;
//     }

//     return <Route path={path} element={<Element />} />;
// };

// export default ProtectedRoute;
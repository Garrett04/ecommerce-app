<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/skeleton/2.0.4/skeleton.min.css">
    <title>Edit Profile</title>
</head>
<body>
    <h1>Edit Profile</h1>
    <h2><%= username %></h2>

    <ul>
        <% if(typeof wrongPassword !== 'undefined') { %>
            <li><%= wrongPassword %></li>
        <% } %>
    </ul>
    
    <form action="/users/<%= userId %>/edit-profile?_method=PUT" method="POST">
        <div>
            <input type="text" id="name" name="name" placeholder="New Name">
        </div>

        <div>
            <input type="password" id="password" name="password" placeholder="Old Password">
        </div>

        <div>
            <input type="password" id="password2" name="password2" placeholder="New Password">
        </div>

        <div>
            <input type="submit" value="Update">
        </div>
    </form>
</body>
</html>